version: '3.8'

services:
  notification-service:
    build: .
    ports:
      - "8082:8082"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    volumes:
      - /home/ubuntu/service_account.json:/app/service_account.json:ro
